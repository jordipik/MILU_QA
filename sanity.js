const fs = require("fs"); function check(p1, p2) { const d1 = JSON.parse(fs.readFileSync(p1, "utf8")); const d2 = JSON.parse(fs.readFileSync(p2, "utf8")); const map1 = new Map(d1.filter(x => x["PART NO."]).map(x => [x["PART NO."], x.designation_gesa])); const map2 = new Map(d2.filter(x => x["PART NO."]).map(x => [x["PART NO."], x.designation_gesa])); console.log(`${p1} entries: ${d1.length}`); console.log(`${p2} entries: ${d2.length}`); console.log(`${p1} distinct PN: ${map1.size}`); console.log(`${p2} distinct PN: ${map2.size}`); let diff = 0; for(let [k,v] of map1) { if(map2.has(k) && map2.get(k) !== v) { if(diff < 3) console.log(`Diff PN ${k}: [${v}] vs [${map2.get(k)}]`); diff++; } } console.log(`Total diffs between files: ${diff}`); } check("engine_12V4000M40A.json", "json_originales/qa_12V4000M40A.json");
