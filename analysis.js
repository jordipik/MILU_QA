const fs = require("fs"); function analyze(enginePath, qaPath) { console.log(`\nAnalyzing ${qaPath} -> ${enginePath}`); const engineData = JSON.parse(fs.readFileSync(enginePath, "utf8")); const qaData = JSON.parse(fs.readFileSync(qaPath, "utf8")); const qaMap = new Map(); const duplicates = []; qaData.forEach(item => { const partNo = item["PART NO."]; if (!partNo) return; const designation = item["designation_gesa"]; if (qaMap.has(partNo)) { if (qaMap.get(partNo) !== designation) { duplicates.push({ partNo, old: qaMap.get(partNo), new: designation }); } } else { qaMap.set(partNo, designation); } }); if (duplicates.length > 0) { console.log(`Found ${duplicates.length} PART NO. duplicates with different designation_gesa in ${qaPath}`); } else { console.log(`No PART NO. duplicates with different designation_gesa in ${qaPath}`); } let matchCount = 0; let changeCount = 0; const realChanges = []; engineData.forEach(item => { const partNo = item["PART NO."]; if (!partNo || !qaMap.has(partNo)) return; matchCount++; const qaDesignation = qaMap.get(partNo); if (String(item["designation_gesa"]) !== String(qaDesignation)) { changeCount++; if (realChanges.length < 5) { realChanges.push({ "PART NO.": partNo, "Engine Designation": item["designation_gesa"], "QA Designation": qaDesignation }); } } }); console.log(`1) Match by PART NO.: ${matchCount} records`); console.log(`2) Real changes (designation_gesa differs): ${changeCount}`); console.log(`4) 5 Examples of real changes:`); console.table(realChanges); } analyze("engine_12V4000M40A.json", "json_originales/qa_12V4000M40A.json"); analyze("engine_12V4000M53.json", "json_originales/qa_12V4000M53.json");
