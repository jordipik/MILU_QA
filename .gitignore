
zz_copias/
Copia_seguridad_v1.01/
# Backups y copias locales fuera del árbol versionado.
zz_copias/backups_repo_root/
# Resultados generados de comparaciones QA PDF.
qa_pdf_compare_engine_*.json
zz_old/
esquemas_pos_circulos/
fotos_articulos/
fotos_motores/
node_modules/

# BD espejo SQLite (Fase E): regenerable desde JSON con `npm run db:import`.
data/db/
# Informes de validación generados (Fase D y E).
data/output/validation/
# Snapshots de datos (DT-3): ficheros grandes, regenerables con npm run data:snapshot.
# Solo se versiona el directorio raíz (con .gitkeep). Los snapshots reales quedan locales.
data/snapshots/*/

# Evitar versionar cambios de datos JSON locales.
*.json

# Backups JSON locales (ej: .json.backup, .json.backup-*, .json.backup.*)
*.json.backup*
